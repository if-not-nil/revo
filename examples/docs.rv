@doc """
  adds two numbers
"""
fn add(a, b) a + b

@doc "greets by name"
const greet = fn(name) "hi " ~ name

@doc "method style docs also work"
fn math:twice(x) x * 2

print(add(2, 3))
print(greet("revo"))
print(math:twice(4))
